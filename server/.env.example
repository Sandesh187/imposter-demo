PORT=3001
CLIENT_ORIGIN=*
LOG_LEVEL=info
NODE_ENV=development

# Optional — uncomment to enable Redis persistence
# Rooms survive server restarts when Redis is configured
# REDIS_URL=redis://localhost:6379
